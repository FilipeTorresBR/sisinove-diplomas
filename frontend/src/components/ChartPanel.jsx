import { ResponsiveContainer, BarChart, Bar, CartesianGrid, XAxis, YAxis, Tooltip, PieChart, Pie, Cell } from 'recharts';
const colors = ['#0d4b8e', '#138a5c', '#c58d00', '#c0392b', '#7d5fff', '#20c997'];
export function BarPanel({ title, data, dataKey = 'total', nameKey = 'name' }) { return <div className="panel"><h3>{title}</h3><div style={{ width: '100%', height: 300 }}><ResponsiveContainer><BarChart data={data}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey={nameKey} /><YAxis /><Tooltip /><Bar dataKey={dataKey} fill="#0d4b8e" radius={[8, 8, 0, 0]} /></BarChart></ResponsiveContainer></div></div>; }
export function PiePanel({ title, data, dataKey = 'total', nameKey = 'name' }) { return <div className="panel"><h3>{title}</h3><div style={{ width: '100%', height: 300 }}><ResponsiveContainer><PieChart><Pie data={data} dataKey={dataKey} nameKey={nameKey} outerRadius={110} label>{data.map((_, i) => <Cell key={i} fill={colors[i % colors.length]} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer></div></div>; }
